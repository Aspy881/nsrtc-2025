<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /nsrtc/
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /nsrtc/index.html [L]
</IfModule>
